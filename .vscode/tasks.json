{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Helloworld Unicode",
            "type": "shell",
            "group": "build",
            "command": "${workspaceFolder}/../NSIS/bin/makensis",
            "args": [
                "${workspaceFolder}/helloworldUnicode.nsi"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}